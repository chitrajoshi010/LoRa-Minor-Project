# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with
code in this repository.

**Canonical agent instructions now live in [`AGENTS.md`](./AGENTS.md)** — read
that file. It covers the project summary, module map, runtime data flow,
build/test commands, architecture rules, LDSE protocol invariants, pin maps,
sensor/acoustic notes, watchdog rule, gotchas, the code-review-graph MCP
tools, and key files. This file is kept only as the entry point Claude Code
looks for; do not duplicate content here — edit `AGENTS.md` instead.

Other reference docs: `ARCHITECTURE.md` (file-by-file map), `FLASHING.md`
(build/flash verification log), `forest_monitor/explain.md` (plain-language
walkthrough), `Minor_project_Mid_term_diffence.md` (academic report).
